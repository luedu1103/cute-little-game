name: cute_game
description: "A cute little game to play!"
publish_to: "none"
version: 1.2.0+3

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.35.1
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  flutter_launcher_icons: ^0.14.4

  # Firestore
  cloud_firestore: ^5.0.0
  firebase_core: ^3.0.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icon/
